# ===========================================
# Al-Haramain Store Frontend - Development Dockerfile
# ===========================================
# This Dockerfile is for local development with hot-reload
# Usage: docker-compose -f ../Al-Haramain-Store/docker-compose.dev.yml up
# ===========================================

FROM node:22-alpine

LABEL maintainer="Al-Haramain Store <dev@alharamain.com>"
LABEL description="Frontend development environment with Vite hot-reload"

# Set working directory
WORKDIR /app

# Install dependencies for better compatibility
RUN apk add --no-cache \
    git \
    python3 \
    make \
    g++

# Copy package files first (for better layer caching)
COPY package*.json ./

# Install dependencies
# Using --legacy-peer-deps for potential React 19 compatibility issues
RUN npm ci --legacy-peer-deps

# Copy the rest of the application
# Note: In development, we mount the source as a volume instead
COPY . .

# Expose Vite dev server port
# Port 3000 is used for Docker, matching backend's docker-compose.dev.yml
EXPOSE 3000

# Environment variables for development
ENV NODE_ENV=development
ENV VITE_PORT=3000

# Start Vite dev server with host binding for Docker
# --host 0.0.0.0 allows external access from host machine
# --port 3000 matches the exposed port and backend config
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "3000"]
